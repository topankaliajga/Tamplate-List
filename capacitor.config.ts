import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'project ke 4',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
